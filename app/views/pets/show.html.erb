<h1>Pet</h1>
<%= @pet.name %>, <%= @pet.species %> found <%= @days_ago %> days ago in <%= @pet.address %>
<%= link_to 'Delete pet', pet_path(@pet), data: { turbo_method: :delete, turbo_confirm: "Are you sure ?" } %>
